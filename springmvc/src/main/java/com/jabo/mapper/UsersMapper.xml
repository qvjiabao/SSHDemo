<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jabo.mapper.UsersMapper">

    <insert id="addModel" parameterType="UserModel">
        insert into books(bookName, bookCounts, detail)
        values (#{bookName}, #{bookCounts}, #{detail})
    </insert>

    <delete id="deletedModelById" parameterType="int">
        delete
        from books
        where bookID = #{bookID}
    </delete>

    <update id="updateModel" parameterType="UserModel">
        update books
        set bookName   = #{bookName},
            bookCounts = #{bookCounts},
            detail     = # {detail}
        where bookID = #{bookID}
    </update>

    <select id="queryModelById" resultType="UserModel">
        select *
        from users
        where user_Id = #{id}
    </select>

    <select id="queryAllModel" resultType="UserModel">
        SELECT *
        from users
    </select>
</mapper>